import { createSlice } from "@reduxjs/toolkit";

const initialState:{

}={

}
const generalSclice = createSlice({
    name:"general",
    initialState,
    reducers:{

    }
})

export const {} = generalSclice.actions
export default generalSclice.reducer
